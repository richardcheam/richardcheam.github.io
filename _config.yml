locale: en-US
title: RICHARD CHEAM
description: My portfolio and knowledge hub.
url: https://richardcheam.github.io
baseurl: ""
timezone: Europe/Paris

markdown: kramdown
permalink: pretty

collections:
  notes:
    output: true
    permalink: /notes/:path/

plugins:
  - jekyll-feed
  - jekyll-sitemap

include:
  - _pages

exclude:
  - node_modules
  - vendor
  - package.json
  - package-lock.json
  - Dockerfile
  - CONTRIBUTING.md

defaults:
  - scope:
      path: "_pages"
      type: pages
    values:
      layout: default
  - scope:
      path: "_notes"
      type: notes
    values:
      layout: default
